# Archived SQLite-era tests

These tests document the pre-PostgreSQL migration behaviour and are intentionally excluded from the active test suite. The production architecture and active integration tests are PostgreSQL-only. Git history preserves the original context.
