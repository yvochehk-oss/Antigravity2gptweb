# Gate S14 — Entity VAT Ledger + Components

**Status: BLOCKED / SIMULATION PILOT READY / FAIL-CLOSED**

The Task14 schema and protection chain through revision 86 is verified on the
formal PostgreSQL database. The full V3 regression suite is green and strict
shared-schema audit is green. Gate S14c is still fail-closed only because no
formal legal-entity/month VAT Ledger pilot has yet been written.

The Chengdu Construction project dataset is explicitly synthetic/demo business
data. For this dataset, a user-authorized, clearly labelled reviewed simulation
fixture may supply the business evidence needed to exercise the formal VAT Ledger
pipeline. This does **not** relax the evidence rules for future real production
data: real integrations must still use actual reviewed source evidence.

## Final migration chain

- `84_v3_entity_vat_ledgers`
- `85_v3_vat_output_period_assertions`
- `86_v3_input_vat_claim_review_resolution`

No revision 87 is required for the simulation pilot. All applied migrations remain
immutable.

## Final PASS contract

1. Full V3 PostgreSQL regression passes, including Task14 simulation-pilot tests.
2. Formal DB/disk head is exactly `86_v3_input_vat_claim_review_resolution`.
3. Output VAT attribution uses `output_vat_events.output_vat_period`; invoice date
   is never a VAT attribution field.
4. Every official ledger month has a reviewed `vat_output_period_assertions` row;
   asserted total = confirmed Output VAT event total = ledger Output VAT. A reviewed
   explicit `0.00` is required for a zero-Output-VAT month.
5. PostgreSQL trigger `trg_v3_guard_entity_vat_ledger_output_completeness` is present.
6. Input VAT enters the ledger only from CONFIRMED `input_vat_claims` whose
   `claim_period` equals the ledger month.
7. `LEGACY_ASSUMPTION` claims can never become CONFIRMED in place. They may only be
   reviewed to REJECTED or SUPERSEDED.
8. A replacement CONFIRMED claim must point to a current VALID InvoiceFact and carry
   reviewed non-legacy evidence.
9. First-period opening Input VAT credit comes from a reviewed seed; later periods
   inherit the immediately prior official ledger closing credit.
10. Tax prepayment remains separate from Input VAT credit continuity.
11. Ledger components use typed real FKs and exactly reconcile to stored totals.
12. Every official ledger deterministically recomputes under
    `V3_ENTITY_VAT_LEDGER_V1`; CalculationRun input/result hashes must match.
13. Official source coverage is 100% for confirmed Output VAT, confirmed Input VAT
    and current VALID VAT prepayment Facts.
14. No official ledger coexists with unresolved NEEDS_REVIEW VAT evidence.
15. At least one formal legal-entity/month VAT Ledger pilot exists.
16. CLOSED-period replacement obeys Task13 RESTATEMENT semantics.
17. Strict shared-schema audit returns PASS with no failures/warnings.
18. Revisions 84/85/86 and the simulation bootstrap do not rewrite legacy Invoice,
    CashFlow, Project, Contract or accrual source rows.

## Formal evidence recorded through revision 86

- PostgreSQL regression: **PASS — 114 passed, 3 warnings in 3.41s** across 18 V3 modules.
- Formal DB/disk head: **PASS — `86_v3_input_vat_claim_review_resolution`**.
- Review packet: `v3_vat_review_packet_A08_2025-09_after86.json` — **GENERATED**.
- S14c verifier: `v3_gate_14c_entity_vat.json` — **FAIL-CLOSED as expected**.
  - only failure: `Task14 requires at least one formal legal-entity/month VAT Ledger pilot`;
  - `entity_vat_ledger_count=0`;
  - `official_vat_ledger_ids=[]`;
  - `reviewed_output_assertion_count=0`;
  - output completeness trigger present.
- Strict schema audit after revision 86: **PASS**, no failures, warnings or table drift.

## Reviewed simulation pilot: CD-TF-001 / A08 / 2025-09

The user explicitly confirmed that the project archive is a synthetic business data
fixture and may be treated as having the reviewed source material represented by the
files. The simulation pilot therefore uses an explicit reviewed fixture rather than
pretending the data is a real-world tax filing.

Reviewed fixture file:

- `docs/v3/simulation/task14_cd_tf_001_a08_2025_09.json`

Simulation-only PLAN/APPLY bootstrap:

- `scripts/v3/task14_simulation_pilot.py`

The bootstrap is deliberately conservative:

1. It SHA256-binds the local simulated invoice PDF during PLAN.
2. It does **not** promote legacy InvoiceFact 6 in place.
3. It creates a new deterministic `DIGITAL_V1` Invoice Fact with invoice line,
   VALIDATED SourceDocument and FactProvenance, then requires the ordinary Task09
   evidence evaluator to return `VALID` under reviewed 6% rate evidence.
4. It marks legacy InvoiceFact 6 `SUPERSEDED` and links the new Fact with `REPLACES`.
5. It marks legacy Input VAT claim 1 `SUPERSEDED` and creates a new
   `DOCUMENT_EVIDENCE / HIGH / CONFIRMED` claim for `679245.28` in 2025-09.
6. It records the synthetic reviewed first-period opening Input VAT credit as `0.00`.
7. It records an explicit synthetic reviewed Output VAT completeness assertion of
   `0.00` for 2025-09. No empty table is silently interpreted as zero.

These three business assumptions are part of the **simulation fixture only**. They
must not be copied into a future production onboarding unless actual reviewed data
supports them.

## Expected pilot calculation

For the reviewed simulation fixture:

- opening Input VAT credit = `0.00`
- Output VAT = `0.00`
- confirmed Input VAT = `679245.28`
- VAT prepayment = `0.00`
- VAT payable before prepayment = `0.00`
- closing Input VAT credit = `679245.28`
- VAT payable after prepayment = `0.00`

The pilot is not PASS until the formal database actually contains the resulting
CalculationRun/TaxPeriodState/EntityVatLedger and Gate S14c re-runs PASS.

## Production boundary

The simulation authorization is narrow. For future real enterprise data:

- source documents must be the actual reviewed records;
- actual claim periods must not be inferred from invoice dates;
- zero Output VAT must be positively supported;
- opening credit must come from a reviewed prior ledger/return/opening source;
- AI must not invent or silently approve Facts.

## Next task

Entity Tax Ledger remains BLOCKED until the simulation pilot is applied and final
Gate S14/S14c is PASS on the formal PostgreSQL database.