# Gate S14 — Entity VAT Ledger + Components

**Status: BLOCKED / REVIEW REQUIRED / FAIL-CLOSED**

Revision 84, the PostgreSQL regression suite and strict shared-schema audit have
passed on the formal PostgreSQL database. The required legal-entity/month VAT
Ledger pilot has **not** been created because formal review found unresolved VAT
evidence. The review also exposed an Output VAT completeness gap, now addressed
additively by Task14b / revision 85. Revision 84 remains immutable.

## Preconditions

- Gate S13 = PASS.
- Revision 84 follows immutable revision 83.
- Final Task14 Gate now requires additive revision 85
  `85_v3_vat_output_period_assertions`.

## Final PASS contract

1. Full V3 PostgreSQL suite including `tests/test_v3_entity_vat_ledgers.py`,
   `tests/test_v3_vat_review_packet.py` and
   `tests/test_v3_vat_output_period_assertions.py` passes.
2. Formal DB/disk head is exactly `85_v3_vat_output_period_assertions`.
3. Output VAT attribution uses `output_vat_events.output_vat_period`; neither
   Output VAT events nor Entity VAT Ledgers use `invoice_date` as a tax-period field.
4. Every official ledger month has a reviewed `vat_output_period_assertions` row.
   Its asserted monthly total must equal both confirmed Output VAT event total and
   `entity_vat_ledgers.output_vat`. Explicit reviewed `0.00` is the only supported
   representation of a proven zero-Output-VAT month.
5. PostgreSQL trigger `trg_v3_guard_entity_vat_ledger_output_completeness` is present
   and prevents bypass of requirement 4.
6. Input VAT enters the ledger only from confirmed `input_vat_claims` whose
   `claim_period` equals the ledger month.
7. Tax prepayment enters only from current VALID VAT `tax_prepayment_facts` and
   reduces payable after Input VAT credit continuity; it never becomes Input VAT credit.
8. First-period opening Input VAT credit comes from a reviewed opening-balance
   seed. Later periods inherit the immediately prior official ledger's closing credit.
9. `entity_vat_ledger_components` uses typed real FKs to Output VAT events,
   Input VAT claims, TaxPrepaymentFacts, prior Ledger or opening seed; no fake
   polymorphic source ID is allowed.
10. Every ledger's component totals exactly equal its stored aggregate fields.
11. Every ledger recomputes exactly under `V3_ENTITY_VAT_LEDGER_V1` and its
    CalculationRun result hash matches the deterministic result payload.
12. Every current official VAT ledger belongs to a SUCCEEDED VAT CalculationRun
    in the same reporting-party/month scope.
13. Every current official ledger covers 100% of confirmed Output VAT events,
    confirmed Input VAT claims and current VALID VAT prepayment Facts for its scope.
14. A current official ledger may not coexist with unresolved `NEEDS_REVIEW`
    Input/Output VAT evidence for that legal-entity/month.
15. At least one formal legal-entity/month VAT Ledger pilot exists.
16. CLOSED-period ledger replacement continues to obey Task13 RESTATEMENT semantics.
17. Strict shared-schema audit returns PASS with no failures or warnings.
18. Revisions 84/85 do not rewrite legacy Invoice/CashFlow/Project/Contract/accrual rows.

## Formal evidence recorded so far

- PostgreSQL regression through review-packet test: **PASS — 104 passed, 3 warnings in 3.60s**.
- Formal DB head before Task14b: **PASS — `84_v3_entity_vat_ledgers`**.
- Discovery: `v3_vat_ledger_discovery.json` — **GENERATED**.
  - current candidate: `A08 / 2025-09`, reporting Party `8`;
  - `eligible=false`;
  - blocker: `unresolved VAT evidence blocks ledger: input_claim_ids=[1], output_event_ids=[]`.
- Read-only review packet: `v3_vat_review_packet_A08_2025-09.json` — **GENERATED**.
- Strict schema audit after revision 84: `v3_schema_audit_after_task14.json` — **PASS**,
  no failures, warnings or table drift.
- Revision 85 PostgreSQL regression / formal migration / S14b Gate: **NOT YET EXECUTED**.
- VAT Ledger PLAN/APPLY result: **NOT GENERATED**.

## A08 / 2025-09 review findings

The review packet establishes these facts without inference:

1. `input_vat_claim_id=1`, amount `679245.28`, remains
   `LEGACY_ASSUMPTION / LOW / NEEDS_REVIEW`.
2. Its linked InvoiceFact is itself `LEGACY_MIGRATION_V1 / NEEDS_REVIEW`, has no
   invoice date/code, and has no Fact provenance row. Therefore the legacy claim
   cannot be promoted directly to CONFIRMED.
3. No confirmed or unresolved Output VAT event exists for the month. This is not
   evidence that Output VAT is zero.
4. No prior official VAT Ledger and no reviewed opening seed exists.
5. Repository search found no source file/metadata for invoice `255182174019` or
   legacy note file `INVOICE_TF-A08-EXT-EXP_255182174019_增值税专票.pdf`; repository
   contents therefore do not support automatic claim confirmation.

## Task14b reviewed completeness evidence

Revision 85 adds `vat_output_period_assertions`. Use
`docs/v3/vat_output_assertion.template.json` and
`scripts/v3/vat_output_assertion.py` only after a human reviewer has verified the
complete monthly Output VAT total. A `0.00` assertion is valid only when zero
Output VAT is positively supported, not merely because the event table is empty.

Revision-85 operational wrappers are:

- `scripts/v3/entity_vat_ledger_85.py`
- `scripts/v3/vat_ledger_evidence_85.py`
- `scripts/v3/vat_review_packet_85.py`
- `scripts/v3/entity_vat_gate_14b.py`

## Important fail-closed rule

S14 remains blocked until all three review areas are supported by real evidence:

1. Input VAT claim 1 is rejected/superseded or replaced by a properly reviewed
   confirmed claim;
2. the first-period opening Input VAT credit is supported by a reviewed seed;
3. the monthly Output VAT total is supported by a reviewed completeness assertion
   whose total matches confirmed Output VAT events.

Do not infer an Output VAT period from invoice date/legacy period, do not promote a
legacy assumption, do not treat an empty Output VAT event table as zero, and do not
invent an opening balance merely to create a pilot.

## Next task

Entity Tax Ledger remains BLOCKED until final S14/S14b is PASS. The VAT ledger is
the legal VAT continuity layer; later managerial tax views must consume it rather
than reconstructing VAT from legacy Invoice dates or deductible flags.
