# Gate S14 — Entity VAT Ledger + Components

**Status: BLOCKED / REVIEW REQUIRED / FAIL-CLOSED**

Revisions 84 and 85 are verified on the formal PostgreSQL database. Formal review
still lacks sufficient evidence for the required legal-entity/month VAT Ledger
pilot. Task14c adds revision 86 so a human review can close a legacy Input VAT
assumption as REJECTED or SUPERSEDED without ever promoting it in place to
CONFIRMED. Revision 86 is code-ready but has not yet been executed on the formal DB.

## Final migration chain

- `84_v3_entity_vat_ledgers`
- `85_v3_vat_output_period_assertions`
- `86_v3_input_vat_claim_review_resolution`

All earlier applied revisions remain immutable.

## Final PASS contract

1. Full V3 PostgreSQL suite including `tests/test_v3_entity_vat_ledgers.py`,
   `tests/test_v3_vat_review_packet.py`, `tests/test_v3_vat_output_period_assertions.py`
   and `tests/test_v3_input_vat_claim_review_resolution.py` passes.
2. Formal DB/disk head is exactly `86_v3_input_vat_claim_review_resolution`.
3. Output VAT attribution uses `output_vat_events.output_vat_period`; neither
   Output VAT events nor Entity VAT Ledgers use `invoice_date` as a tax-period field.
4. Every official ledger month has a reviewed `vat_output_period_assertions` row;
   asserted total = confirmed Output VAT event total = ledger output VAT. A reviewed
   `0.00` is required to represent a proven zero-Output-VAT month.
5. PostgreSQL trigger `trg_v3_guard_entity_vat_ledger_output_completeness` is present.
6. Input VAT enters the ledger only from CONFIRMED `input_vat_claims` whose
   `claim_period` equals the ledger month.
7. `LEGACY_ASSUMPTION` Input VAT claims can never be CONFIRMED. Revision 86 permits
   human-reviewed `REJECTED` or `SUPERSEDED` resolution and requires reviewer/time.
8. A SUPERSEDED legacy claim may only be replaced operationally through the reviewed
   PLAN/APPLY workflow; replacement CONFIRMED claims require a current VALID
   InvoiceFact plus real MANUAL_REVIEW or DOCUMENT_EVIDENCE.
9. Tax prepayment remains separate from Input VAT credit continuity.
10. First-period opening Input VAT credit comes from a reviewed opening seed; later
    periods inherit the immediately prior official ledger closing credit.
11. Ledger components use typed real FKs and component totals exactly reconcile.
12. Every official ledger deterministically recomputes under
    `V3_ENTITY_VAT_LEDGER_V1`, including CalculationRun input/result hashes.
13. Official ledger source coverage is 100% for confirmed Output VAT, confirmed
    Input VAT and current VALID VAT prepayment Facts.
14. No official ledger coexists with unresolved NEEDS_REVIEW VAT evidence.
15. At least one formal legal-entity/month VAT Ledger pilot exists.
16. CLOSED-period replacement obeys Task13 RESTATEMENT semantics.
17. Strict shared-schema audit returns PASS without failures/warnings.
18. Revisions 84/85/86 do not rewrite legacy Invoice/CashFlow/Project/Contract/accrual rows.

## Formal evidence recorded through revision 85

- PostgreSQL regression: **PASS — 109 passed, 3 warnings in 4.50s** across 17 V3 modules.
- Formal DB/disk head: **PASS — `85_v3_vat_output_period_assertions`**.
- Review packet: `v3_vat_review_packet_A08_2025-09_after85.json` — **GENERATED**.
- S14b verifier: `v3_gate_14b_entity_vat.json` — **FAIL-CLOSED as expected**.
  - only failure: required formal VAT Ledger pilot does not yet exist;
  - `entity_vat_ledger_count=0`;
  - `official_vat_ledger_ids=[]`;
  - `reviewed_output_assertion_count=0`;
  - completeness trigger present.
- Strict schema audit after revision 85: **PASS**, no failures/warnings/table drift.
- Revision 86 regression/migration/S14c verifier: **NOT YET EXECUTED**.

## A08 / 2025-09 evidence blockers

1. `input_vat_claim_id=1`, amount `679245.28`, remains
   `LEGACY_ASSUMPTION / LOW / NEEDS_REVIEW`.
2. Linked InvoiceFact remains `LEGACY_MIGRATION_V1 / NEEDS_REVIEW`, with no invoice
   date/code and no Fact provenance; it cannot support a replacement CONFIRMED claim.
3. No Output VAT event exists; an empty event table is not proof of zero Output VAT.
4. No prior official VAT Ledger and no reviewed opening seed exists.
5. Repository and connected Dropbox searches found no source file/metadata for invoice
   `255182174019` / `INVOICE_TF-A08-EXT-EXP_255182174019_增值税专票.pdf`.

## Task14c reviewed Input VAT resolution

Revision 86 changes only the resolution state machine for legacy Input VAT assumptions:

- `NEEDS_REVIEW -> REJECTED` with reviewer/time; or
- `NEEDS_REVIEW -> SUPERSEDED` atomically with a new evidence-backed CONFIRMED claim.

It still prohibits `LEGACY_ASSUMPTION -> CONFIRMED`.

Operational files:

- `scripts/v3/vat_input_claim_resolution.py`
- `docs/v3/vat_input_claim_resolution.template.json`
- `scripts/v3/entity_vat_ledger_86.py`
- `scripts/v3/vat_ledger_evidence_86.py`
- `scripts/v3/vat_review_packet_86.py`
- `scripts/v3/vat_output_assertion_86.py`
- `scripts/v3/entity_vat_gate_14c.py`

## Important fail-closed rule

S14 remains blocked until real reviewed evidence resolves all three areas:

1. claim 1 is REJECTED or validly SUPERSEDED/replaced;
2. the opening Input VAT credit is supported;
3. the complete monthly Output VAT total is supported.

Do not infer any of these values merely to create a pilot.

## Next task

Entity Tax Ledger remains BLOCKED until final S14/S14c is PASS.
