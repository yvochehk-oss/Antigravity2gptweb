# Gate S14 — Entity VAT Ledger + Components

**Status: BLOCKED / REVIEW REQUIRED / FAIL-CLOSED**

Revision 84, the PostgreSQL regression suite and strict shared-schema audit have
passed on the formal PostgreSQL database. The required legal-entity/month VAT
Ledger pilot has **not** been created because formal discovery correctly found
unresolved VAT evidence for the only current candidate scope.

## Preconditions

- Gate S13 = PASS.
- Revision 84 follows immutable revision 83.

## PASS contract

1. Full V3 PostgreSQL suite including `tests/test_v3_entity_vat_ledgers.py` passes.
2. Formal DB/disk head is exactly `84_v3_entity_vat_ledgers`.
3. Output VAT attribution uses `output_vat_events.output_vat_period`; neither
   Output VAT events nor Entity VAT Ledgers use `invoice_date` as a tax-period field.
4. Input VAT enters the ledger only from confirmed `input_vat_claims` whose
   `claim_period` equals the ledger month.
5. Tax prepayment enters only from current VALID VAT `tax_prepayment_facts` and
   reduces payable after Input VAT credit continuity; it never becomes Input VAT credit.
6. First-period opening Input VAT credit comes from a reviewed opening-balance
   seed. Later periods inherit the immediately prior official ledger's closing credit.
7. `entity_vat_ledger_components` uses typed real FKs to Output VAT events,
   Input VAT claims, TaxPrepaymentFacts, prior Ledger or opening seed; no fake
   polymorphic source ID is allowed.
8. Every ledger's component totals exactly equal its stored aggregate fields.
9. Every ledger recomputes exactly under `V3_ENTITY_VAT_LEDGER_V1` and its
   CalculationRun result hash matches the deterministic result payload.
10. Every current official VAT ledger belongs to a SUCCEEDED VAT CalculationRun
    in the same reporting-party/month scope.
11. Every current official ledger covers 100% of confirmed Output VAT events,
    confirmed Input VAT claims and current VALID VAT prepayment Facts for its scope.
12. A current official ledger may not coexist with unresolved `NEEDS_REVIEW`
    Input/Output VAT evidence for that legal-entity/month.
13. At least one formal legal-entity/month VAT Ledger pilot exists.
14. CLOSED-period ledger replacement continues to obey Task13 RESTATEMENT semantics.
15. Strict shared-schema audit returns PASS with no failures or warnings.
16. Revision 84 does not rewrite legacy Invoice/CashFlow/Project/Contract/accrual rows.

## Formal evidence recorded

- PostgreSQL regression: **PASS — 103 passed, 3 warnings in 3.30s**.
- Formal DB/disk head: **PASS — `84_v3_entity_vat_ledgers`**.
- Discovery: `v3_vat_ledger_discovery.json` — **GENERATED**.
  - only current candidate: `A08 / 2025-09`, reporting Party `8`;
  - `eligible=false`;
  - blocker: `unresolved VAT evidence blocks ledger: input_claim_ids=[1], output_event_ids=[]`.
- Strict schema audit: `v3_schema_audit_after_task14.json` — **PASS**, no failures or warnings,
  no unexplained DB-only tables and no table drift.
- Reviewed evidence plan/result: **NOT YET AVAILABLE**.
- VAT Ledger PLAN/APPLY result: **NOT GENERATED**.
- Final Gate output: **NOT PASS** because the mandatory formal pilot does not yet exist.

## Review required before pilot

For `A08 / 2025-09`, do not infer or auto-promote any evidence. Human-reviewed
source evidence must resolve all of the following before the ledger can be built:

1. `input_vat_claim_id=1` is still `LEGACY_ASSUMPTION / LOW / NEEDS_REVIEW` and
   therefore cannot enter confirmed Input VAT.
2. No confirmed Output VAT attribution event currently exists for the scope.
   Output VAT period must not be inferred from Invoice date or legacy period.
3. Because this is the first official VAT Ledger period for the reporting Party,
   a reviewed opening Input VAT credit seed is required unless a prior official
   Ledger becomes available.

Use `scripts/v3/vat_review_packet.py` to generate a read-only evidence packet for
human review. The packet is descriptive only and never creates or confirms tax facts.

## Important fail-closed rule

S14 remains blocked until discovery returns a genuinely eligible scope and one
formal VAT Ledger pilot passes the S14 verifier. Do not invent an opening balance,
infer Output VAT period from Invoice date, promote a legacy assumption, or ignore
unresolved VAT evidence merely to create a pilot.

## Next task

Entity Tax Ledger remains BLOCKED until S14 is PASS. The VAT ledger is the legal
VAT continuity layer; later managerial tax views must consume it rather than
reconstructing VAT from legacy Invoice dates or deductible flags.
