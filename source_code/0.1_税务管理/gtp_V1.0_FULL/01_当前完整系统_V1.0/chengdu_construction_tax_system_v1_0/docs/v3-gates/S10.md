# Gate S10 — Input VAT Claims

**Status: NOT EXECUTED / FAIL-CLOSED**

Task10 implementation is present, but this Gate must not be marked PASS until
revision 80, the PostgreSQL regression suite, one formal entity-month pilot and
the strict schema audit all pass on the real PostgreSQL database.

## Preconditions

- Gate S1 = PASS.
- Gate S07A = PASS.
- Gate S07B = PASS.
- Gate S08 = PASS.
- Gate S09 = PASS.
- Revision 80 must follow immutable revision 79.

## PASS contract

1. Full V3 PostgreSQL suite including `tests/test_v3_input_vat_claims.py` passes.
2. Formal DB/disk head is exactly `80_v3_input_vat_claims`.
3. `input_vat_claims` has real FKs to InvoiceFact, InternalEntity reporting
   party and optional SourceDocument.
4. Claim table contains no `project_id`, `invoice_date`, `deductible` or
   `legacy_period` field.
5. `claim_period` is a month-start date and is the only Input VAT attribution
   period.
6. `LEGACY_ASSUMPTION` rows can only be `LOW + NEEDS_REVIEW`.
7. No `LEGACY_ASSUMPTION` row is `CONFIRMED`.
8. Confirmed claims may exist only on current `VALID` Invoice Facts and must
   resolve to the reviewed VAT reporting party for the entire claim month.
9. One discovered eligible legal-entity/month has 100% legacy invoice-map
   coverage and one review claim per candidate Invoice Fact.
10. One entity-month reconciliation has zero unexplained residual.
11. Formal Task10 pilot creates no confirmed Input VAT from legacy assumptions.
12. `scripts/v3_schema_audit.py --strict` passes after revision 80.

## Expected formal pilot behavior

The first Task10 legacy pilot is intentionally conservative:

```text
legacy invoices.direction = in
legacy invoices.deductible = true
legacy invoices.period = candidate claim_period assumption only
        ↓
input_vat_claims
claim_status = NEEDS_REVIEW
evidence_type = LEGACY_ASSUMPTION
confidence = LOW
```

These candidates do **not** contribute to confirmed Input VAT.

## Evidence placeholders

- PostgreSQL regression: **NOT RUN / NOT RECORDED**
- Formal DB/disk head 80: **NOT VERIFIED**
- Discovery: `v3_input_vat_discovery.json` — **NOT GENERATED**
- PLAN: `v3_input_vat_plan.json` — **NOT GENERATED**
- APPLY result: `v3_input_vat_result.json` — **NOT GENERATED**
- Gate output: `v3_gate_10_input_vat.json` — **NOT GENERATED**
- Strict schema audit: `v3_schema_audit_after_task10.json` — **NOT GENERATED**

## Next task

No next gated schema task may start until S10 is PASS. In particular, do not
build Entity VAT Ledgers from Invoice date or legacy deductible flags.
