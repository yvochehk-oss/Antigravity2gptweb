# Gate S10 — Input VAT Claims

**Status: PASS**

Task10 has passed on the formal PostgreSQL database `projectrag` on 2026-08-30.
Revision 80, the full PostgreSQL regression suite, one legal-entity/month pilot,
Task10 Gate verification and strict shared-schema audit all passed.

## Preconditions

- Gate S1 = PASS.
- Gate S07A = PASS.
- Gate S07B = PASS.
- Gate S08 = PASS.
- Gate S09 = PASS.
- Revision 80 follows immutable revision 79.

## Formal evidence

- PostgreSQL regression: **PASS — 79 passed / 0 failed / 3 warnings in 2.49s**.
- Formal DB/disk head: **`80_v3_input_vat_claims`**.
- Discovery selected exactly one eligible entity-month:
  - entity `A08`, Party 8, reporting Party 8;
  - claim period `2025-09`;
  - one mapped legacy input candidate;
  - legacy VAT candidate total `679245.28`;
  - blockers `[]`.
- PLAN digest: `14002c97e10ab70ebf15a3e6237b98314da571873e8b19217914a730b86d0f5d`.
- APPLY created claim id `1` for legacy invoice `252` / InvoiceFact `6` with:
  - `claim_status = NEEDS_REVIEW`;
  - `evidence_type = LEGACY_ASSUMPTION`;
  - `confidence = LOW`;
  - `claim_period = 2025-09-01`.
- APPLY reconciliation:
  - legacy candidate VAT = `679245.28`;
  - review candidate VAT = `679245.28`;
  - confirmed linked VAT = `0.00`;
  - reporting-period confirmed Input VAT = `0.00`;
  - explained residual = `0.00`.
- `scripts/v3/input_vat_gate_10.py`: **PASS**, `failures=[]`.
- `legacy_assumption_confirmed_count = 0`.
- `confirmed_nonvalid_invoice_count = 0`.
- `confirmed_exceeds_invoice_vat_count = 0`.
- `confirmed_reporting_mismatch_ids = []`.
- prohibited claim columns = `[]`.
- `scripts/v3_schema_audit.py --strict`: **PASS**, `failures=[]`, `warnings=[]`, `table_drift={}`.

## Architectural result

Legacy `invoices.deductible=true` did not create confirmed Input VAT. Legacy
`period` was used only as a reviewed-candidate claim-period assumption. Formal
Input VAT attribution is now exclusively driven by `input_vat_claims.claim_period`
and only `CONFIRMED` claim events may contribute to later VAT ledgers.

## Gate decision

All S10 PASS conditions are satisfied. The next additive schema task may start.
Do not derive Entity VAT Ledger Input VAT from Invoice date or legacy deductible
flags; later ledgers must consume confirmed claim events only.
