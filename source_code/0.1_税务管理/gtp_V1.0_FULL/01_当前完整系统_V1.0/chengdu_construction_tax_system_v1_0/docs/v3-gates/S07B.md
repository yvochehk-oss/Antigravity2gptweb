# Gate S07B — Invoice Red / Void / Relationships

**Status: FAIL-CLOSED / NOT YET EXECUTED**

Task 07b implementation is present, but revision `78_v3_invoice_fact_relationships` and its PostgreSQL contract tests have not yet been executed against the formal/test databases in the evidence available to this record.

## Required evidence

- PostgreSQL `alembic upgrade head` including revision 78
- PostgreSQL pytest output including `tests/test_v3_fact_invoice_relationships.py`
- `v3_gate_07b_invoice_relationships.json` from the read-only verifier
- confirmation that production readers still use legacy `invoices`

## Gate conditions

- `fact_relationships` exists with true FKs to `facts`
- relationship types limited to `REVERSAL_OF / REPLACES / VOID_RELATION / CORRECTS`
- no self or duplicate relationships
- no relationship cycles
- RED Invoice Facts carry non-positive amounts
- every RED Invoice Fact has explicit `REVERSAL_OF`
- reversal source/target parties and currency are consistent
- VOIDED Invoice Facts retain stored amounts but do not enter projection
- every VOIDED Invoice Fact has `VOID_RELATION`
- `invoice_medium / invoice_category / invoice_status` are separate axes
- no `red_blue_flag` or `original_invoice_id` duplicate truth columns

**Task 08 legacy Invoice trial backfill remains blocked until this gate is PASS or explicitly approved EXPLAINED.**
