# Gate S12 — Project Tax Treatment / Tax Prepayment Facts

**Status: NOT EXECUTED / FAIL-CLOSED**

Task12 implementation is present, but this Gate must not be marked PASS until
revision 82, the full PostgreSQL regression suite, the read-only S12 verifier
and the strict shared-schema audit all pass on the formal PostgreSQL database.

## Preconditions

- Gate S11 = PASS.
- Revision 82 must follow immutable revision 81.

## PASS contract

1. Full V3 PostgreSQL suite including `tests/test_v3_project_tax_prepayment.py`
   passes.
2. Formal DB/disk head is exactly `82_v3_project_tax_prepayment`.
3. `project_tax_treatments` has real Project/InternalEntity FKs and
   non-overlapping effective windows for each `(project_id, tax_type)`.
4. Project tax treatment contains no numeric rate / prepayment-rate column.
5. `tax_prepayment_facts` is a real `facts` subtype with Project and
   InternalEntity reporting-party FKs.
6. TaxPrepayment contains no cashflow/bank/invoice/accrual columns.
7. Tax period is month-start and event signs satisfy PREPAYMENT/REVERSAL/
   ADJUSTMENT semantics.
8. Every TaxPrepayment subtype row belongs to `facts.fact_type='TAX_PREPAYMENT'`.
9. A current `VALID` TaxPrepayment Fact must link a reviewed treatment whose
   project, tax type and reporting party match and whose effective window
   contains `tax_event_date`.
10. A non-current TaxPrepayment Fact may not remain `validation_status='VALID'`.
11. Revision 82 does not migrate or rewrite legacy Project/CashFlow/Progress/
    Invoice/Contract data.
12. Strict shared-schema audit returns PASS with no failures or warnings.

## Evidence placeholders

- PostgreSQL regression: **NOT RUN / NOT RECORDED**
- Formal DB/disk head 82: **NOT VERIFIED**
- Gate output: `v3_gate_12_project_tax.json` — **NOT GENERATED**
- Strict schema audit: `v3_schema_audit_after_task12.json` — **NOT GENERATED**

Zero treatment/prepayment rows are acceptable for the initial Gate because
Task12 is additive schema/domain construction and intentionally performs no
legacy backfill.

## Next task

Calculation Runs / Tax Period States remain BLOCKED until S12 is PASS. Do not
use CashFlow as a TaxPrepayment substitute and do not hard-code project
prepayment rates to make a later calculation appear complete.
