# Gate S12 — Project Tax Treatment / Tax Prepayment Facts

**Status: PASS**

Formal PostgreSQL evidence on `projectrag` confirms Task12 is complete.

## Preconditions

- Gate S11 = PASS.
- Revision 82 follows immutable revision 81.

## Formal evidence — 2026-08-30

- PostgreSQL regression: **PASS — 92 passed / 0 failed / 3 warnings in 2.84s**.
- Formal DB head: `82_v3_project_tax_prepayment`.
- Disk head: `82_v3_project_tax_prepayment`.
- Gate output: `v3_gate_12_project_tax.json` — **PASS / failures=[]**.
- Strict schema audit: `v3_schema_audit_after_task12.json` — **PASS / failures=[] / warnings=[]**.
- Schema audit metadata table count: 68; Tax metadata tables: 51; RAG metadata tables: 22.
- Missing metadata-owned tables: 0.
- Unexplained DB-only tables: 0.
- Table drift: none.

Gate S12 evidence:

- `project_tax_treatment_count = 0`.
- `tax_prepayment_fact_count = 0`.
- `treatment_prohibited_columns = []`.
- `prepayment_prohibited_columns = []`.
- `wrong_fact_type_count = 0`.
- `invalid_tax_period_count = 0`.
- `invalid_event_sign_count = 0`.
- `valid_without_treatment_count = 0`.
- `valid_unreviewed_treatment_count = 0`.
- `valid_treatment_mismatch_count = 0`.
- `noncurrent_valid_fact_count = 0`.

Zero treatment/prepayment rows are acceptable because Task12 is additive schema/domain construction and intentionally performs no legacy backfill.

## Contract retained

1. `project_tax_treatments` uses real Project/InternalEntity FKs and non-overlapping effective windows per `(project_id, tax_type)`.
2. No numeric rate / prepayment-rate column is stored in project tax treatment.
3. `tax_prepayment_facts` is a real `facts` subtype with Project and InternalEntity reporting-party FKs.
4. TaxPrepayment contains no cashflow/bank/invoice/accrual columns.
5. Current VALID TaxPrepayment Facts require a reviewed, matching, effective treatment.
6. Non-current TaxPrepayment Facts may not remain VALID.
7. Revision 82 did not migrate or rewrite legacy Project/CashFlow/Progress/Invoice/Contract rows.

## Next task

Gate S12 permits Task13: Calculation Runs / Tax Period States. Closed periods must never be silently overwritten; changes after close must be represented by explicit restatement runs.
