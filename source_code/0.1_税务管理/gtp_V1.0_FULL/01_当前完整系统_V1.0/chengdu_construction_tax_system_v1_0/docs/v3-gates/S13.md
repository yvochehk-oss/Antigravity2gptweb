# Gate S13 — Calculation Runs / Tax Period States

**Status: NOT EXECUTED / FAIL-CLOSED**

Task13 implementation is present, but this Gate must not be marked PASS until revision 83, the full PostgreSQL regression suite, the read-only S13 verifier and strict shared-schema audit all pass on the formal PostgreSQL database.

## Preconditions

- Gate S12 = PASS.
- Revision 83 must follow immutable revision 82.

## PASS contract

1. Full V3 PostgreSQL suite including `tests/test_v3_calculation_period_states.py` passes.
2. Formal DB/disk head is exactly `83_v3_calculation_runs_period_states`.
3. `calculation_runs` and `tax_period_states` have real InternalEntity/run FKs and contain no project/cash/invoice/accrual axis columns.
4. Tax periods are month-start dates.
5. RESTATEMENT runs require `supersedes_run_id`; STANDARD runs forbid it.
6. Terminal CalculationRun rows are immutable after completion.
7. A period's current run must be SUCCEEDED and have the same reporting-party/tax-type/tax-period scope.
8. OPEN periods cannot carry closed anchor metadata and may only point to STANDARD current runs.
9. CLOSED -> OPEN is rejected by PostgreSQL.
10. The first close anchors `closed_run_id` to a SUCCEEDED STANDARD current run.
11. CLOSED-period current-run replacement is accepted only for a SUCCEEDED RESTATEMENT that directly supersedes the prior current run.
12. Every CLOSED period's current-run supersedes chain deterministically reaches its original `closed_run_id`, with all intermediate rows SUCCEEDED RESTATEMENT runs in the same scope.
13. Strict shared-schema audit returns PASS with no failures or warnings.
14. Revision 83 does not create actual ledger results or close any formal period automatically.

## Evidence placeholders

- PostgreSQL regression: **NOT RUN / NOT RECORDED**
- Formal DB/disk head 83: **NOT VERIFIED**
- Gate output: `v3_gate_13_calculation_periods.json` — **NOT GENERATED**
- Strict schema audit: `v3_schema_audit_after_task13.json` — **NOT GENERATED**

Zero CalculationRun / TaxPeriodState rows are acceptable for the initial formal Gate. The state-machine behavior is verified on the disposable PostgreSQL test database; production periods must not be closed just to satisfy a migration Gate.

## Next task

Entity VAT Ledger construction remains BLOCKED until S13 is PASS. Ledger rebuild logic must use CalculationRun/TaxPeriodState semantics and must never replace a CLOSED-period official result without a RESTATEMENT run.
