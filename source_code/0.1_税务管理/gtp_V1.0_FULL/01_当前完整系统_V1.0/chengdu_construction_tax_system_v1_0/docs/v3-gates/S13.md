# Gate S13 — Calculation Runs / Tax Period States

**Status: PASS**

Task13 has completed formal PostgreSQL validation on database `projectrag`.

## Preconditions

- Gate S12 = PASS.
- Revision 83 follows immutable revision 82.

## PASS evidence — 2026-08-30

1. Full V3 PostgreSQL regression suite including `tests/test_v3_calculation_period_states.py`: **97 passed, 0 failed, 3 warnings in 3.96s**.
2. Formal DB/disk head: **`83_v3_calculation_runs_period_states`**.
3. `scripts/v3/calculation_period_gate_13.py`: **PASS**, `failures: []`.
4. `scripts/v3_schema_audit.py --strict`: **PASS**, `failures: []`, `warnings: []`, `table_drift: {}`.
5. PostgreSQL protection triggers are present:
   - `trg_v3_guard_calculation_run_update`
   - `trg_v3_guard_tax_period_state`
6. Formal database intentionally contains zero CalculationRun/TaxPeriodState rows at this migration Gate; no production period was closed merely to prove the schema.

## Formal Gate output

`v3_gate_13_calculation_periods.json` reported:

- `calculation_run_count = 0`
- `tax_period_state_count = 0`
- `closed_period_count = 0`
- `invalid_run_period_count = 0`
- `invalid_terminal_run_count = 0`
- `invalid_restatement_link_count = 0`
- `invalid_current_scope_state_ids = []`
- `invalid_open_state_ids = []`
- `invalid_closed_chain_state_ids = []`

The disposable PostgreSQL test suite verifies the state-machine behavior: CLOSED periods cannot reopen, STANDARD runs cannot silently replace a CLOSED result, and a CLOSED-period replacement requires a SUCCEEDED RESTATEMENT that directly supersedes the prior current run. Terminal CalculationRun rows are immutable.

## Strict shared-schema audit

Formal strict audit after revision 83 reported:

- metadata tables: 70
- Tax metadata tables: 53
- RAG metadata tables: 22
- DB tables: 80
- missing tables: none
- unexplained DB-only tables: none
- Tax head DB/disk: `83_v3_calculation_runs_period_states`
- RAG head DB/disk: `019_timezone_aware_timestamps`
- warnings: none
- table drift: none

## Next task

**Task14 — Entity VAT Ledger + Components is unblocked.** Ledger rebuilds must use CalculationRun/TaxPeriodState semantics. A CLOSED-period official result may only change through an explicit RESTATEMENT run; Input VAT must be attributed by `input_vat_claims.claim_period`, and Output VAT must not be inferred from Invoice date alone.
