# Gate S11 — Contract / Fulfillment Facts

**Status: PASS**

Task11 has passed on the formal PostgreSQL database `projectrag`.

## Preconditions

- Gate S10 = PASS.
- Revision 81 follows immutable revision 80.

## PASS evidence — 2026-08-30

- PostgreSQL regression suite: **86 passed / 0 failed / 3 warnings in 2.89s**.
- Formal Tax DB/disk head: **`81_v3_contract_fulfillment_facts`**.
- `scripts/v3/contract_fulfillment_gate_11.py`: **PASS**, `failures: []`.
- `contract_fact_count = 0`; zero rows are valid because Task11 is an additive schema/domain boundary task and does not migrate legacy contracts.
- `fulfillment_fact_count = 0`; zero rows are valid for the same reason.
- `contract_prohibited_columns = []`.
- `fulfillment_prohibited_columns = []`.
- `contract_wrong_fact_type_count = 0`.
- `contract_same_party_count = 0`.
- `fulfillment_wrong_fact_type_count = 0`.
- `fulfillment_same_party_count = 0`.
- `invalid_contract_link_count = 0`.
- Strict shared-schema audit: **PASS**, `failures: []`, `warnings: []`, `table_drift: {}`.
- Strict audit Tax DB/disk heads both equal revision 81; RAG DB/disk heads both equal `019_timezone_aware_timestamps`.

## Verified contract

1. `contract_facts` and `fulfillment_facts` have real Fact/Party FKs.
2. `contract_facts` contains no `project_id`, `internal_trade`, `buyer_code` or `seller_code`.
3. `fulfillment_facts` contains no `project_id`, `counterparty_code`, `evidence_complete` or `internal_trade`.
4. Contract buyer/seller cannot resolve to the same Party.
5. Fulfillment performing/receiving parties cannot resolve to the same Party.
6. Fact subtype type checks pass.
7. Nullable `contract_fact_id` remains valid for independent Fulfillment extraction.
8. Revision 81 did not modify or migrate legacy Contract/Fulfillment rows.

## Next task

**Task12 / revision 82 — Project Tax Treatment + Tax Prepayment Facts is UNBLOCKED.**
Project tax treatment remains separate from Contract/Fulfillment facts; project allocation is not backfilled into those subtype tables.
