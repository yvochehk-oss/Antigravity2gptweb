# Gate S11 — Contract / Fulfillment Facts

**Status: NOT EXECUTED / FAIL-CLOSED**

Task11 implementation is present, but this Gate must not be marked PASS until
revision 81, the full PostgreSQL regression suite, the read-only S11 verifier
and strict shared-schema audit all pass on the formal PostgreSQL database.

## Preconditions

- Gate S10 = PASS.
- Revision 81 must follow immutable revision 80.

## PASS contract

1. Full V3 PostgreSQL suite including
   `tests/test_v3_contract_fulfillment_facts.py` passes.
2. Formal DB/disk head is exactly `81_v3_contract_fulfillment_facts`.
3. `contract_facts` and `fulfillment_facts` have real Fact/Party FKs.
4. `contract_facts` contains no `project_id`, `internal_trade`, `buyer_code` or
   `seller_code`.
5. `fulfillment_facts` contains no `project_id`, `counterparty_code`,
   `evidence_complete` or `internal_trade`.
6. Contract buyer/seller cannot resolve to the same Party.
7. Fulfillment performing/receiving parties cannot resolve to the same Party.
8. Every ContractFact row belongs to a `facts.fact_type='CONTRACT'` row.
9. Every FulfillmentFact row belongs to a `facts.fact_type='FULFILLMENT'` row.
10. A nullable `contract_fact_id` remains valid for independent Fulfillment
    extraction; any non-null link must resolve to `contract_facts`.
11. Strict shared-schema audit returns PASS with no failures or warnings.
12. Revision 81 does not modify or migrate legacy Contract/Fulfillment rows.

## Evidence placeholders

- PostgreSQL regression: **NOT RUN / NOT RECORDED**
- Formal DB/disk head 81: **NOT VERIFIED**
- Gate output: `v3_gate_11_contract_fulfillment.json` — **NOT GENERATED**
- Strict schema audit: `v3_schema_audit_after_task11.json` — **NOT GENERATED**

## Next task

Project Tax Treatment / Tax Prepayment schema remains BLOCKED until S11 is PASS.
Do not introduce project allocation into ContractFact or FulfillmentFact to make
that later task easier; the separation is intentional.
