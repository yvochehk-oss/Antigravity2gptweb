# Gate S07A — Minimal Fact + Invoice Core

**Status: FAIL-CLOSED / NOT YET EXECUTED**

Task 07a implementation is present on `feature/v3-database-core`, but revision
77 and the new PostgreSQL contract tests have not yet been executed against the
formal/test databases in the evidence available to this record.

## Required evidence

- PostgreSQL `alembic upgrade head` including `77_v3_fact_core_invoice`
- PostgreSQL pytest output including `tests/test_v3_fact_invoice_core.py`
- `v3_gate_07a_fact_invoice.json` from the read-only verifier
- confirmation that production readers still use legacy `invoices`

## Gate conditions

- required Task 07a tables present
- `invoice_identity_key` UNIQUE
- no `direction/internal_trade/deductible/project_id` truth columns on InvoiceFact
- internal physical invoice represented by one Fact
- duplicate digital identity rejected
- DRAFT insert works
- unbalanced header/lines cannot become VALID
- VALID rows have no header/line integrity violations
- no orphan true FKs

**Task 07b red/void relationships remains blocked until this gate is PASS.**
