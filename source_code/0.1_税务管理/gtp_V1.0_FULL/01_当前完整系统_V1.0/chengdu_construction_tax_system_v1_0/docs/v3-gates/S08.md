# Gate S08 — Legacy Invoice Small-Batch Pilot

**Status: FAIL-CLOSED / NOT YET EXECUTED**

Task 08 implementation is present on `feature/v3-database-core`, but revision 79,
the PostgreSQL Task 08 test suite, the saved pilot plan, the formal-database
pilot apply, and the read-only S08 verifier have not yet been executed in the
evidence available to this record.

## Required evidence

- PostgreSQL `alembic upgrade head` including `79_v3_legacy_invoice_pilot_bridge`
- PostgreSQL pytest output including `tests/test_v3_legacy_invoice_pilot.py`
- reviewed `v3_invoice_pilot_plan.json`
- formal-database `v3_invoice_pilot_result.json`
- `v3_gate_08_invoice_pilot.json` from `scripts/v3/invoice_gate_08.py`
- confirmation production readers still use legacy `invoices`

## Gate conditions

- selected pilot legacy rows have 100% `legacy_invoice_map` coverage
- zero silent drops
- deterministic internal pairs are exactly two perspectives -> one InvoiceFact
- ambiguous/conflicting groups do not invent Facts
- single perspectives remain `NEEDS_REVIEW`
- all Task 08-created Facts remain `NEEDS_REVIEW`
- all Task 08-created InvoiceFacts use `LEGACY_MIGRATION_V1`
- header net/VAT/gross reconciles to preserved legacy evidence
- `real_cost_invoice_links.invoice_fact_id` preserves old cost provenance
- no duplicate invoice identity
- legacy `invoice_id` provenance and legacy readers remain intact

**Task 09 invoice validation remains blocked until this gate is PASS or an explicitly approved EXPLAINED pass.**
