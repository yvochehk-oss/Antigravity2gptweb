# 成都建工 IDP V3.0 local runtime

# Primary semantic extraction model: Windows V3 canonical local LLM endpoint.
LING_ENABLED=1
LING_BASE_URL=http://127.0.0.1:8930/v1
LING_MODEL=spark-x2.5-4b
LING_API_KEY=local
LING_TIMEOUT_SECONDS=120

# OCR: engine is initialized only when a scan/image actually needs OCR
OCR_ENABLED=1
OCR_LANG=ch
OCR_PDF_DPI=180

# Optional second-line audit model. OFF by default on low-resource hosts.
GRANITE_ENABLED=0
GRANITE_BASE_URL=http://127.0.0.1:8001/v1
GRANITE_MODEL=granite-4.2-3b
GRANITE_API_KEY=local
GRANITE_TIMEOUT_SECONDS=180
# Optional business materiality threshold. Empty/0 disables amount-only triggering.
GRANITE_MIN_AMOUNT=

# IDP shares the project's canonical portable PostgreSQL database on Windows.
DATABASE_URL=postgresql://postgres@127.0.0.1:54320/projectrag

# API boundary: with no key, only loopback clients are accepted. Set a long random
# key for any reverse proxy/LAN integration and send it as X-IDP-API-Key.
IDP_API_KEY=
IDP_LOCALHOST_ONLY=1
IDP_MAX_UPLOAD_BYTES=26214400

# Original document storage
STORE_ORIGINALS=1
IDP_STORAGE_DIR=./storage/originals
