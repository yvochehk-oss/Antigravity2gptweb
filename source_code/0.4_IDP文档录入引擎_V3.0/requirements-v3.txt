fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
PyMuPDF>=1.24,<2.0
python-multipart>=0.0.9,<1.0
python-dotenv>=1.0,<2.0
httpx>=0.27,<1.0
psycopg[binary]>=3.2,<4.0

# OCR is optional and lazy-loaded only for scans/images:
# paddleocr
# paddlepaddle

# Ling-3.0-tiny and optional Granite 4.2 3B are served by local
# OpenAI-compatible inference endpoints; their Python runtimes are not part of
# the IDP process. MinerU remains an optional complex-PDF fallback only.
