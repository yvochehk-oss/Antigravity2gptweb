成都建工 V2.0 正式 PostgreSQL RAG 014 证据包完整性约束修复审计
============================================================

执行日期：2026-08-24（Asia/Shanghai）
目标数据库：projectrag（localhost:5432）
目标用户：yvoche
执行目录：V2.0/source_code/0.2_RAG系统/project-rag-v1.1
范围：RAG 013_ai_review_evidence_pack_fk -> 014_ai_review_evidence_pack_checks
禁止范围：未连接或操作 projectrag_xianyu；未 seed；未写入正式业务测试数据；未提交 Git。

一、发现与修复原因
------------------

正式库在本次任务开始时已经处于 RAG 013_ai_review_evidence_pack_fk，
但实际 public.rag_evidence_packs 缺少源码/迁移合同声明的两个 CHECK 约束：

1. ck_rag_evidence_packs_status：status 只能为 AVAILABLE、EMPTY、DEGRADED；
2. ck_rag_evidence_packs_evidence_count：evidence_count >= 0。

该漂移会使数据库接受非法证据包状态或负数证据数量，属于数据完整性问题。
正式库当时证据包记录数为 0，且无活动事务，因此可安全补充约束。

二、修复前备份
--------------

备份文件：projectrag_formal_pre_rag014_20260824_190320+0800.dump
路径：/Users/yvoche/AI开发/073_成都建工/V2.0/database/backups/projectrag_formal_pre_rag014_20260824_190320+0800.dump
格式：PostgreSQL custom（pg_dump --format=custom --no-owner --no-privileges）
SHA256：80b23a011c6d96c89df04fd785cc1bc06f44cb78aa5aad75f0121afe90f52cb9
校验文件：projectrag_formal_pre_rag014_20260824_190320+0800.dump.sha256
TOC 列表：projectrag_formal_pre_rag014_20260824_190320+0800.dump.list
校验结果：SHA256 和 pg_restore --list 均通过；TOC entries=606。

备份前关键状态：

- RAG Alembic：013_ai_review_evidence_pack_fk；单版本行；
- rag_evidence_packs：已存在，记录数 0；
- 两个目标 CHECK：0；
- 其他数据库会话 active=0、idle in transaction=0、waiting=0；
- entities=26（legal_entity=25，非独立法人=1），A04 -> A03；
- projects=6，entity_code IS NULL 的项目=6；
- 未操作 projectrag_xianyu。

三、代码变更
------------

新增：

alembic/versions/014_ai_review_evidence_pack_checks.py

行为：

- 仅允许 PostgreSQL；
- 检查表和目标字段存在；
- 添加 CHECK 前先读取并拒绝非法 status/负数 evidence_count；
- 使用幂等的约束名称，避免重复创建；
- downgrade 只删除本迁移创建的约束，不删除表或业务数据。

新增测试：

tests/test_ai_review_evidence_pack_checks_migration.py

四、正式库迁移
--------------

执行：013 -> 014，设置 lock_timeout=5s、statement_timeout=30s。
结果：PASS。

迁移后只读核验：

- alembic_version_rag=014_ai_review_evidence_pack_checks；
- 两个 CHECK 约束均存在；
- ai_review_runs.rag_evidence_pack_id -> rag_evidence_packs.id，ON DELETE SET NULL；
- rag_evidence_packs.project_id -> projects.id，ON DELETE CASCADE；
- 两个目标索引均存在；
- evidence pack 记录=0；孤儿 Evidence Pack/Run 引用=0；
- entities=26、legal entities=25、legacy A/B/C/D 代码=0；
- A04 为非独立法人且 parent_entity_code=A03；
- projects=6、主体为空映射=6；
- 关键业务表计数与迁移前已知状态一致：documents=60、chunks=60、invoices=58、tax_ledgers=25、facts_snapshots=0、ai_review_runs=0；
- 迁移后 active=0、idle in transaction=0、waiting=0；
- 8921/8922 监听端口均为空。

五、隔离库迁移证明
------------------

隔离数据库：projectrag_codex_rag014_20260824（非正式库）。

- 从正式库迁移前备份恢复；
- 014 up：版本变为 014，两个 CHECK 出现；
- 014 down：版本回到 013，表保留，两个 CHECK 消失；
- 再次 up：版本回到 014，两个 CHECK 恢复；
- 插入一个非法 status/负数 probe 后执行 up：迁移按预期拒绝，版本仍为 013、约束未部分落地；
- 清理 probe 后再次 up 成功。

隔离验证结论：UP/DOWN/RE-UP PASS；非法数据 fail-closed PASS；正式库未写入 probe。

六、测试
--------

- uv run ruff check 目标迁移和测试：PASS；
- uv run pytest -q tests/test_ai_review_evidence_pack_checks_migration.py tests/test_ai_review_evidence_pack.py：7 passed；
- python3 -m compileall 目标迁移和测试：PASS；
- PostgreSQL 隔离库 up/down/re-up：PASS；
- PostgreSQL 非法数据拒绝和事务回滚保持版本：PASS；
- 正式库迁移后只读数据完整性核验：PASS。

七、边界与剩余事项
------------------

1. 本次执行前正式库已经是 013；本次没有重复执行 012->013，也没有声称重新证明此前 013 迁移过程。上述 pre_rag014 备份可恢复到本次迁移前的正式 013 状态。
2. 正式库 tax_ledgers 当前为 25，不是历史验收单中提到的 43；本次 RAG 约束修复未修改 Tax 数据。
3. 迁移期间停止了已核实路径的 V2.0 8921/8922 uvicorn 子进程；本次没有自动重启服务。启动应由主 Agent/操作者在确认数据库状态后执行 V2.0/start_all.sh。

结论：RAG 014 正式库约束修复 PASS。数据库当前已与 V2.0 Evidence Pack ORM/迁移合同一致，未操作闲鱼建工数据库，未写入正式业务测试数据，未提交 Git。
